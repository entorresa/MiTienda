---
### 3. Dependencias del entorno Python
---

Dentro del entorno de Python desde donde se ejecuta Odoo, se precisan las algunas librerías, mismas que se pueden instalar con el comando:

```sh
pip install markdown weasyprint pymdown-extensions beautifulsoup4
```
